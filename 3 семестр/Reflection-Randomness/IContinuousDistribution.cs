using System;

namespace Reflection.Randomness
{
    public interface IContinuousDistribution
    {
        double Generate(Random rnd);
    }
}

namespace Reflection.Randomness2
{
    public interface IContinuousDistribution
    {
        double Generate(Random rnd);
    }
}

namespace Reflection.Randomness3
{
    public interface IContinuousDistribution
    {
        double Generate(Random rnd);
    }
}